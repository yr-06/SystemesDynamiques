CC= gcc
CFLAGS= -Wall -g #-std=c11
LIB= ../../libs
LDFLAGS= -lpoint -lm
INC= ../../include
EXEC= li.exe
SRC= li.c
OBJ= $(SRC:.c=.o)

all : $(EXEC)

$(EXEC) : li.c
	$(CC) $< -o $@ $(CFLAGS) $(LDFLAGS) -I $(INC) -L $(LIB)

