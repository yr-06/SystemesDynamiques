CC= gcc
CFLAGS= -Wall -g #-std=c11
LIB= ../../libs
LDFLAGS= -lpoint -lm
INC= ../../include
EXEC= rossler.exe
SRC= rossler.c
OBJ= $(SRC:.c=.o)

all : $(EXEC)

$(EXEC) : rossler.c
	$(CC) $< -o $@ $(CFLAGS) $(LDFLAGS) -I $(INC) -L $(LIB)

